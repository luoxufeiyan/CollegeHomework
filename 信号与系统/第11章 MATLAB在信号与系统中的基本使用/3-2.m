clear all;
close all;
t=-3:0.001:3;
A=0.5;
a=-2;
h=0.001;
ft=A*exp(a*t);
ft0=A*exp(a*(t-1));
subplot(3,1,1);
plot (t,ft);
title('ft');
y1=diff(ft)/h;
subplot(3,1,2);
plot (t(1:length(t)-1),y1);
title('dt');
y2=diff((ft0-1))/h;
subplot(3,1,3);
plot (t(1:length(t)-1),y2);
title('d(t-1)');